# Dadav

## helm-schema ([releases](https://github.com/dadav/helm-schema/releases))

```yaml
ownbrew:
  packages:
    - name: helm-schema
      tap: foomo/tap/dadav/helm-schema
      version: 0.14.1
```
